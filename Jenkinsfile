pipeline {
    agent any
    stages{
        stage ('Git check out') {
            steps {
                git branch: 'main', url: 'https://github.com/Strvsuri/USEFUL-demo-counter-app.git'
            }
        }
    }
}
